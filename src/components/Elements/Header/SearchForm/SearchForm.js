import React from "react";
import {Input} from "fronton-react";

const SearchForm = () => {
    return (
        <Input
            placeholder="Search"
            inputSize="m"
        />
    )
}

export default SearchForm;